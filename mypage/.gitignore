node_modules
.next
out
build
storage
*.db
*.db-journal
.env
.env.local
next-env.d.ts
.DS_Store

# Incremental TypeScript build cache. Regenerated by every build, so tracking it
# meant a permanently dirty tree and a guaranteed conflict between machines.
tsconfig.tsbuildinfo

# Playwright: session cookies written by tests/auth.setup.ts, plus run output.
tests/.auth
playwright-report
test-results
.playwright-cli
